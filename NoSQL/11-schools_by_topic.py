#!/usr/bin/env python3
"""Select with specifics df sdfdsf sd"""

def schools_by_topic(mongo_collection, topic):
    """Select with pymongo fsdf dsf dfs"""
   return list(mongo_collection.find({ "topic": topic}))
